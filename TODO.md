Add a storage system database where all the ai responses are stored so they dont have to be regenerated.

Make better UI for website and extension

Add score rating system (green -> red) for each site
